main();function main(){	//Make certain that user interaction (display of dialogs, etc.) is turned on.	//app.scriptPreferences.userInteractionLevel = UserInteractionLevels.interactWithAll;	if (app.documents.length != 0){		var myDocument = app.activeDocument;		var selection = app.selection;		var swatch = myDocument.swatches.itemByName('マーカー');		if(swatch == null){			alert ("「マーカー」という名前のスウォッチがありません");		}		for(var i=0; i<selection.length; i++){			if(selection[i].constructor.name == 'Text'){				selection[i].underline = true;				selection[i].underlineColor = swatch;				selection[i].underlineWeight = 3;				selection[i].underlineOffset = -0.9;			}		}	} else { 	   alert ("ドキュメントを開いてください");	}}